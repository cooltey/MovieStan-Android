<?xml version="1.0" encoding="utf-8"?>
<android.support.design.widget.CoordinatorLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="com.moviestan.app.LaunchActivity">



    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:gravity="center"
        android:orientation="vertical"
        android:background="@color/white">

        <ImageView
            android:id="@+id/app_logo"
            android:layout_width="@dimen/launch_app_size"
            android:layout_height="@dimen/launch_app_size"
            android:scaleType="centerInside"
            android:src="@drawable/ic_launcher"/>

        <com.facebook.login.widget.LoginButton
            android:id="@+id/login_button"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="@dimen/general_margin_fifty"
            android:layout_marginBottom="@dimen/general_margin_thirty"/>

        <include layout="@layout/dialog_progress"/>
    </LinearLayout>

</android.support.design.widget.CoordinatorLayout>